---
import type { Feature } from "@/types/site-config";

interface Props {
	features?: Feature[];
	title?: string;
	subtitle?: string;
}

const { 
	features = [
		{
			title: "Professional Service",
			description: "High-quality solutions tailored to your specific business needs and requirements.",
			icon: "🎯",
		},
		{
			title: "Expert Team",
			description: "Experienced professionals dedicated to delivering exceptional results for your projects.",
			icon: "👥",
		},
		{
			title: "Reliable Support",
			description: "Ongoing support and maintenance to ensure your solutions continue to perform optimally.",
			icon: "🛠️",
		},
	],
	title = "Why Choose Us",
	subtitle = "We combine expertise, innovation, and dedication to deliver outstanding results for every project."
} = Astro.props;
---

<section class="py-16 bg-white">
  <div class="container mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-3xl font-bold text-gray-900 mb-4">
        {title}
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        {subtitle}
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {features.map((feature) => (
        <div class="text-center p-6 rounded-lg hover:shadow-lg transition-shadow">
          {feature.icon && <div class="text-4xl mb-4">{feature.icon}</div>}
          <h3 class="text-xl font-semibold mb-3">{feature.title}</h3>
          <p class="text-gray-600">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>